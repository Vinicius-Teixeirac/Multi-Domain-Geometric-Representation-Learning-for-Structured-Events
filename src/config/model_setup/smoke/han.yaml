graph:
  type: heterogeneous
  num_neighbors:
  - 5
  - 5
  node_features: all
model:
  name: han
  hidden_dim: 32
  num_layers: 2
  heads: 8
  dropout: 0.3
training:
  device: cuda
  batch_size: 32
  epochs: 5
  lr: 0.005
  weight_decay: 0.005
  patience: 5
  monitor_metric: f1_macro
