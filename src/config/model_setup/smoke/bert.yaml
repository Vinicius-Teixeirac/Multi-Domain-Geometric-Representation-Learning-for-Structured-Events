model:
  model_name: bert-base-uncased
  freeze_until_layer: 10
text:
  max_length: 64
training:
  batch_size: 32
  epochs: 2
  lr: 0.0001
  weight_decay: 0.001
  patience: 5
  monitor_metric: f1_macro
  num_workers: 0
  device: cuda
