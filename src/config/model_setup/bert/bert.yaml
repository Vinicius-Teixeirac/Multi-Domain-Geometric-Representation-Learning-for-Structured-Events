# src/configs/model_setup/bert/bert_base.yaml

model:
  model_name: bert-base-uncased
  num_classes: 4
  freeze_until_layer: 10

text:
  max_length: 256

training:
  batch_size: 256
  epochs: 20
  lr: 0.0001
  weight_decay: 0.001
  patience: 10
  device: cuda

data:
  split:
    tag: default
