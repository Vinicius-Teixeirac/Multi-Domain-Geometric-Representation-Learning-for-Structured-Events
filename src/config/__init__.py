"""Configuration package: filesystem layout (paths.py) and declarative schemas
(schema/) describing dataset columns, encodings, and the fixed column subset
used across preprocessing, representation, and modeling stages."""
