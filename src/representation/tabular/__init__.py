"""Tabular representation package: encoders (label/hash/one-hot), value
transformers (dates, geo-coordinates, scaling), and the pipeline that fits
and applies them to produce model-ready numeric feature tensors."""
